#write a python program that accepts the radius of the circle from the user and computes the area
from math import pi
r = float(input("ENTER THE RADIUS OF THE CIRCLE :"))
radius = pi*r*r
print(radius)